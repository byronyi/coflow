allprojects {
    apply plugin: 'java'

    group 'coflow'
    version '0.0.1'

    sourceCompatibility = 1.7

    repositories {
        mavenCentral()
    }

    dependencies {
        testCompile group: 'junit', name: 'junit', version: '4.11'
    }
}
